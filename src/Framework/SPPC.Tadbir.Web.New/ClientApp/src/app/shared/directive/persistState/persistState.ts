import { Directive, ElementRef, HostListener, Input, OnInit, OnDestroy, Host } from '@angular/core';
import { SessionKeys, BrowserStorageService } from '@sppc/shared/services';
import { DefaultComponent, AutoGeneratedGridComponent } from '@sppc/shared/class';

@Directive({
  selector: '[PersistState]',
  providers: [AutoGeneratedGridComponent,DefaultComponent]
})
export class PersistStateDirective implements OnInit, OnDestroy {

  
  constructor(private element: ElementRef, public bStorageService: BrowserStorageService, @Host() public defaultComponent: AutoGeneratedGridComponent) {
    
  }  

  ngOnInit() {
    
  }

  ngAfterContentInit() {   
    //var viewId = this.defaultComponent.viewId;
    //var elemId = this.element.nativeElement.id;

  }

  ngOnDestroy(): void {
    
  }
}
